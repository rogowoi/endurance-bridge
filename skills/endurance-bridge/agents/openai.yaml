interface:
  display_name: "Endurance Bridge"
  short_description: "Connect and use endurance data through MCP"
  default_prompt: "Use $endurance-bridge to review my training from last week."
dependencies:
  tools:
    - type: "mcp"
      value: "endurance-bridge"
      description: "Private endurance data and training operations"
      transport: "streamable_http"
      url: "https://endurance-bridge-api.vercel.app/api/mcp"
